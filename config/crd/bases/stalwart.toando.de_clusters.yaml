---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clusters.stalwart.toando.de
spec:
  group: stalwart.toando.de
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the clusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Cluster
            properties:
              dataStore:
                description: Datastore the stalwart instances should use
                properties:
                  foundationDb:
                    properties:
                      clusterFile:
                        description: Path to the cluster file for the FoundationDB
                          cluster
                        type: string
                      datacenterId:
                        description: Data center ID
                        type: string
                      machineId:
                        description: Machine ID in the FoundationDB cluster
                        type: string
                      transactionRetryDelay:
                        description: Transaction maximum retry delay
                        type: integer
                      transactionRetryLimit:
                        description: Transaction maximum retry delay
                        maximum: 1000
                        minimum: 1
                        type: integer
                      transactionTimeout:
                        description: Transaction timeout
                        type: integer
                    type: object
                  mysql:
                    properties:
                      allowInvalidCerts:
                        description: Allow invalid TLS certificates when connecting
                          to the store
                        type: boolean
                      authSecret:
                        description: Password to connect to the store
                        type: string
                      authUsername:
                        description: Username to connect to the store
                        type: string
                      database:
                        description: Name of the database
                        type: string
                      host:
                        description: Hostname of the database server
                        type: string
                      maxAllowedPacket:
                        description: Maximum size of a packet in bytes
                        maximum: 1073741824
                        minimum: 1024
                        type: integer
                      poolMaxConnections:
                        description: Maximum number of connections to the store
                        maximum: 8192
                        minimum: 1
                        type: integer
                      poolMinConnections:
                        description: Minimum number of connections to the store
                        maximum: 8192
                        minimum: 1
                        type: integer
                      port:
                        description: Port of the database server
                        maximum: 65535
                        minimum: 1
                        type: integer
                      timeout:
                        description: Connection timeout to the database
                        type: integer
                      useTls:
                        description: Use TLS to connect to the store
                        type: boolean
                    required:
                    - authSecret
                    - host
                    type: object
                  postgresql:
                    properties:
                      allowInvalidCerts:
                        description: Allow invalid TLS certificates when connecting
                          to the store
                        type: boolean
                      authSecret:
                        description: Password to connect to the store
                        type: string
                      authUsername:
                        description: Username to connect to the store
                        type: string
                      database:
                        description: Name of the database
                        type: string
                      host:
                        description: Hostname of the database server
                        type: string
                      options:
                        description: Additional connection options
                        type: string
                      poolMaxConnections:
                        description: Maximum number of connections to the store
                        maximum: 8192
                        minimum: 1
                        type: integer
                      poolRecyclingMethod:
                        description: Method to use when recycling connections in the
                          pool
                        type: string
                      port:
                        description: Port of the database server
                        maximum: 65535
                        minimum: 1
                        type: integer
                      timeout:
                        description: Connection timeout to the database
                        type: integer
                      useTls:
                        description: Use TLS to connect to the store
                        type: boolean
                    required:
                    - authSecret
                    - host
                    type: object
                  rocksDb:
                    properties:
                      blobSize:
                        description: Minimum size of a blob to store in the blob store,
                          smaller blobs are stored in the metadata store
                        maximum: 1048576
                        minimum: 1024
                        type: integer
                      bufferSize:
                        description: Total amount of memory shared by the write buffers
                          (memtables) of every column family. Higher values batch
                          more writes in memory before they are flushed to disk
                        maximum: 4294967296
                        minimum: 8388608
                        type: integer
                      cacheSize:
                        description: Total amount of memory shared by the block caches
                          of every column family, used to cache data blocks, indexes
                          and bloom filters read from disk
                        maximum: 17179869184
                        minimum: 8388608
                        type: integer
                      path:
                        description: Path to the RocksDB data directory
                        type: string
                      poolWorkers:
                        description: Number of worker threads to use for the store,
                          defaults to the number of cores
                        maximum: 64
                        minimum: 1
                        type: integer
                    required:
                    - path
                    type: object
                  sqlite:
                    properties:
                      path:
                        description: Path to the SQLite data directory
                        type: string
                      poolMaxConnections:
                        description: Maximum number of connections to the store
                        maximum: 8192
                        minimum: 1
                        type: integer
                      poolWorkers:
                        description: Number of worker threads to use for the store,
                          defaults to the number of cores
                        maximum: 64
                        minimum: 1
                        type: integer
                    required:
                    - path
                    type: object
                  type:
                    enum:
                    - RocksDb
                    - Sqlite
                    - FoundationDb
                    - PostgreSql
                    - MySql
                    type: string
                required:
                - type
                type: object
              instances:
                default: 1
                description: Number of instances in the cluster
                minimum: 1
                type: integer
            required:
            - dataStore
            - instances
            type: object
          status:
            description: status defines the observed state of Cluster
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
