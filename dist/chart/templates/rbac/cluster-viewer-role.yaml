{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "stalwart-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "stalwart-operator.resourceName" (dict "suffix" "cluster-viewer-role" "context" $) }}
rules:
- apiGroups:
  - stalwart.toando.de
  resources:
  - clusters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - stalwart.toando.de
  resources:
  - clusters/status
  verbs:
  - get
{{- end }}
